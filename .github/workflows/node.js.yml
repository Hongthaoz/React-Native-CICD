name: React Native CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Thiết lập Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18.x'
        cache: 'yarn'

    # Cài đặt Java 17
    - name: Set up Java 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    # Cài đặt dependencies
    - run: yarn install

    # Thiết lập Android SDK
    - name: Set up Android SDK
      uses: android-actions/setup-android@v2
      with:
        api-levels: 33
        build-tools: 33.0.0
        ndk: 21.4.7075529

    # Cấp quyền thực thi cho gradlew
    - name: Grant execution permissions for gradlew
      run: chmod +x android/gradlew

    # Giải mã keystore
    - name: Decode Keystore
      run: |
        echo ${{ secrets.MYAPP_UPLOAD_STORE_FILE }} | base64 --decode > android/app/my-upload-key.keystore

    # Clean Gradle Cache
    - name: Clean Gradle Cache
      run: |
        cd android
        ./gradlew clean

    # Xây dựng APK
    - name: Build APK
      run: |
        cd android
        ./gradlew assembleRelease --stacktrace --info

    # Upload APK
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: android-release-apk
        path: android/app/build/outputs/apk/release/app-release.apk

    # Tùy chọn: Cung cấp thông tin để upload lên Google Play
    # - name: Upload to Google Play
    #   uses: r0adkll/upload-google-play@v1
    #   with:
    #     serviceAccountJson: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT }}
    #     packageName: com.yourapp.package
    #     releaseFiles: android/app/build/outputs/apk/release/app-release.apk
    #     track: production
